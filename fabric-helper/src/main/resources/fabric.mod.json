{
  "schemaVersion": 1,
  "id": "opanel",
  "version": "${version}",
  "environment": "server",
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": "~${minecraft_version}",
    "java": ">=17",
    "fabric": "*"
  }
}
